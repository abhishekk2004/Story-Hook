<div class="container">
    <div class="container">
        <div class="card">
            <div class="card-content">
                <h4>{{story.title}}</h4>
                <div class="divider"></div><br>
                <p>{{story.content}}</p>
            </div>
        </div>
    </div>
</div>